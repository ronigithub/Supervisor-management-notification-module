version: "3.0"

services:
  backend-service:
    build:
      context: backend-service
      dockerfile: Dockerfile
    ports:
      - 8080:8080
      
  frontend:
    build:
      context: frontend
      dockerfile: Dockerfile
    ports:
      - 3000:3000
